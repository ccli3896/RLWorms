README

Each line has roughly 5 h of data. Line 281 (AIY channel) is partial because in the first iteration the data were rotated and saved within the .pkl file. For all other lines this is done only during training. The full AIY dataset is available upon request.